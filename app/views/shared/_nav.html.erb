<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <ul class="nav nav-tabs">
        <li class="">
          <%= link_to "The Imperial Enquirer", root_path, class: 'Logo' %>
        </li>
          <%= render partial: 'shared/category_links' %>
        <li class="pull-right">
          <% if logged_in?(:admin) %>
            <div>
              <%= link_to 'New News Article', new_news_article_path %>
            </div>
            <div>
              <%= render partial: "news_articles/admin_actions" %>
            </div>
          <% elsif logged_in?(:editor) %>
            <div>
              <%= link_to 'New News Article', new_news_article_path %>
            </div>
          <% end %>
          <% if logged_in?(:user) || logged_in?(:editor) || logged_in?(:admin) %>
            <div> <%=link_to "Sign Out", destroy_user_session_path,  method: :delete, class: 'nav-link' %>
          <% else %>
            <div> <%=link_to "Log In", new_user_session_path, class: 'nav-link' %>
            <div> <%=link_to "Register", new_user_registration_path, class: 'nav-link' %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</div>